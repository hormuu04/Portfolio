# 🚀 คู่มือ Deploy บน Vercel (แบบละเอียด)

## ✅ สิ่งที่ต้องเตรียม

1. ✅ โปรเจกต์พร้อม build (`npm run build` ทำงานได้)
2. ✅ ไฟล์ `vercel.json` พร้อมแล้ว
3. ✅ GitHub account (แนะนำ) หรือ GitLab/Bitbucket

---

## 📋 วิธีที่ 1: Deploy ผ่านเว็บไซต์ Vercel (แนะนำ - ง่ายที่สุด)

### ขั้นตอนที่ 1: สมัคร/Login Vercel

1. ไปที่ **[vercel.com](https://vercel.com)**
2. คลิก **"Sign Up"** หรือ **"Log In"**
3. เลือก **"Continue with GitHub"** (แนะนำ) หรือ GitLab/Bitbucket
4. อนุญาตให้ Vercel เข้าถึง repository ของคุณ

### ขั้นตอนที่ 2: สร้าง Project ใหม่

1. หลังจาก login แล้ว คลิก **"Add New..."** → **"Project"**
2. คุณจะเห็นรายการ repositories จาก GitHub
3. **ค้นหาและเลือก repository "Phulaport"** (หรือชื่อ repo ของคุณ)
4. คลิก **"Import"**

### ขั้นตอนที่ 3: ตั้งค่า Build Configuration

Vercel จะ auto-detect Vite แต่ตรวจสอบให้แน่ใจว่า:

- **Framework Preset:** `Vite` (ควร detect อัตโนมัติ)
- **Root Directory:** `./` (ปล่อยว่างหรือใส่ `./`)
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install` (default)

### ขั้นตอนที่ 4: Environment Variables (ถ้ามี)

ถ้าโปรเจกต์ใช้ environment variables:
- คลิก **"Environment Variables"**
- เพิ่มตัวแปรที่ต้องการ (เช่น `VITE_API_URL`)

### ขั้นตอนที่ 5: Deploy!

1. คลิก **"Deploy"**
2. รอสักครู่ (ประมาณ 1-2 นาที)
3. เมื่อเสร็จแล้ว Vercel จะให้ URL:
   - **Production URL:** `phulaport.vercel.app` (หรือชื่อที่คุณตั้ง)
   - **Preview URL:** สำหรับแต่ละ commit

### ✅ เสร็จแล้ว!

เว็บไซต์ของคุณจะอยู่ที่: **`https://phulaport.vercel.app`**

---

## 💻 วิธีที่ 2: Deploy ผ่าน Vercel CLI

### ขั้นตอนที่ 1: ติดตั้ง Vercel CLI

```bash
npm install -g vercel
```

หรือใช้ npx (ไม่ต้องติดตั้ง):

```bash
npx vercel
```

### ขั้นตอนที่ 2: Login

```bash
vercel login
```

จะเปิดเบราว์เซอร์ให้คุณ login

### ขั้นตอนที่ 3: Deploy

```bash
# Deploy แบบ preview (ทดสอบ)
vercel

# Deploy แบบ production
vercel --prod
```

### ขั้นตอนที่ 4: ตอบคำถาม

CLI จะถามคำถาม:
- **Set up and deploy?** → `Y`
- **Which scope?** → เลือก account ของคุณ
- **Link to existing project?** → `N` (ครั้งแรก) หรือ `Y` (ถ้ามีแล้ว)
- **Project name?** → `phulaport` (หรือชื่อที่ต้องการ)
- **Directory?** → `.` (Enter เพื่อใช้ default)

---

## 🔄 Auto Deploy (อัตโนมัติ)

หลังจาก deploy ครั้งแรกแล้ว:

- ✅ **ทุกครั้งที่ push code ไป GitHub** → Vercel จะ deploy อัตโนมัติ
- ✅ **Production:** เมื่อ push ไป `main` หรือ `master` branch
- ✅ **Preview:** เมื่อ push ไป branch อื่น

---

## ⚙️ ตั้งค่าเพิ่มเติม

### เปลี่ยนชื่อ Domain

1. ไปที่ Project Settings
2. คลิก **"Domains"**
3. เพิ่ม custom domain (ถ้ามี)

### ตั้งค่า Environment Variables

1. Project Settings → **"Environment Variables"**
2. เพิ่มตัวแปรที่ต้องการ
3. เลือก Environment (Production, Preview, Development)

### ดู Logs และ Analytics

- **Deployments:** ดูประวัติการ deploy
- **Analytics:** ดูสถิติการใช้งาน (ต้อง upgrade plan)
- **Logs:** ดู error logs

---

## 🐛 แก้ไขปัญหา

### ปัญหา: Build ล้มเหลว

**ตรวจสอบ:**
```bash
# ทดสอบ build ในเครื่องก่อน
npm run build
```

**แก้ไข:**
- ตรวจสอบ error ใน Vercel dashboard
- ดู Build Logs
- ตรวจสอบว่า dependencies ติดตั้งครบ

### ปัญหา: หน้าเว็บไม่แสดงเมื่อรีเฟรช (404)

**แก้ไข:** ไฟล์ `vercel.json` ควรมีอยู่แล้ว แต่ตรวจสอบว่า:

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### ปัญหา: Assets ไม่แสดง

**ตรวจสอบ:**
- ไฟล์อยู่ในโฟลเดอร์ `public` หรือไม่
- ใช้ path ที่ถูกต้อง (เช่น `/image.png`)

---

## 📝 Checklist ก่อน Deploy

- [ ] รัน `npm run build` สำเร็จ
- [ ] ทดสอบด้วย `npm run preview`
- [ ] ไฟล์ `vercel.json` มีอยู่
- [ ] Push code ไป GitHub แล้ว
- [ ] ไม่มี error ใน console

---

## 🎉 หลังจาก Deploy สำเร็จ

1. **แชร์ URL** ให้คนอื่นดูได้เลย
2. **Custom Domain:** ตั้งค่า domain ของคุณเอง
3. **Auto Deploy:** ทุกครั้งที่ push code จะ deploy อัตโนมัติ

---

## 💡 Tips

- ✅ Vercel ฟรีสำหรับ personal projects
- ✅ มี SSL certificate อัตโนมัติ
- ✅ CDN เร็วมาก
- ✅ Preview deployments สำหรับทุก branch
- ✅ Analytics และ monitoring (บางฟีเจอร์ต้อง upgrade)

---

## 🔗 ลิงก์ที่มีประโยชน์

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation](https://vercel.com/docs)
- [Vite + Vercel Guide](https://vercel.com/guides/deploying-vite-with-vercel)

---

**พร้อมแล้ว! ไป deploy กันเลย! 🚀**

