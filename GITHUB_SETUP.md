# 📚 คู่มือ GitHub สำหรับ Deploy (เริ่มต้นจากศูนย์)

## 📋 สารบัญ
1. [สร้าง GitHub Account](#1-สร้าง-github-account)
2. [ติดตั้ง Git](#2-ติดตั้ง-git)
3. [สร้าง Repository บน GitHub](#3-สร้าง-repository-บน-github)
4. [Push Code ไป GitHub](#4-push-code-ไป-github)
5. [ตรวจสอบว่า Code อยู่บน GitHub แล้ว](#5-ตรวจสอบว่า-code-อยู่บน-github-แล้ว)

---

## 1. สร้าง GitHub Account

### ขั้นตอนที่ 1: ไปที่ GitHub

1. เปิดเบราว์เซอร์ไปที่ **[github.com](https://github.com)**
2. คลิก **"Sign up"** (มุมขวาบน)

### ขั้นตอนที่ 2: ลงทะเบียน

1. **กรอกข้อมูล:**
   - Username (ชื่อผู้ใช้)
   - Email address
   - Password

2. **ยืนยันอีเมล** (GitHub จะส่งอีเมลให้)

3. **เลือก Plan:**
   - เลือก **"Free"** (ฟรี)

4. **ตอบคำถาม** (optional - ข้ามได้)

### ✅ เสร็จแล้ว! คุณมี GitHub account แล้ว

---

## 2. ติดตั้ง Git

### ตรวจสอบว่ามี Git แล้วหรือยัง

เปิด **PowerShell** หรือ **Command Prompt** แล้วพิมพ์:

```bash
git --version
```

### ถ้ายังไม่มี Git

#### สำหรับ Windows:

1. **ดาวน์โหลด Git:**
   - ไปที่ [git-scm.com/download/win](https://git-scm.com/download/win)
   - ดาวน์โหลดและติดตั้ง

2. **ตั้งค่า Git (ครั้งแรก):**

เปิด PowerShell หรือ Git Bash แล้วรัน:

```bash
git config --global user.name "ชื่อของคุณ"
git config --global user.email "your-email@example.com"
```

**ตัวอย่าง:**
```bash
git config --global user.name "John Doe"
git config --global user.email "john@example.com"
```

### ✅ ตรวจสอบการติดตั้ง

```bash
git --version
```

ควรแสดงเวอร์ชันของ Git (เช่น `git version 2.40.0`)

---

## 3. สร้าง Repository บน GitHub

### ขั้นตอนที่ 1: ไปที่ GitHub

1. Login เข้า GitHub
2. คลิก **"+"** (มุมขวาบน) → **"New repository"**

### ขั้นตอนที่ 2: ตั้งค่า Repository

1. **Repository name:** `Phulaport` (หรือชื่อที่ต้องการ)
2. **Description:** (optional) เช่น "My Portfolio Website"
3. **Visibility:**
   - ✅ **Public** (แนะนำ - ฟรีและ deploy ง่าย)
   - หรือ **Private** (ถ้าต้องการซ่อนโค้ด)

4. **อย่าเลือก:**
   - ❌ Add a README file (เรามีโค้ดอยู่แล้ว)
   - ❌ Add .gitignore
   - ❌ Choose a license

5. คลิก **"Create repository"**

### ✅ คุณจะเห็นหน้า repository เปล่า พร้อมคำแนะนำ

---

## 4. Push Code ไป GitHub

### ขั้นตอนที่ 1: เปิด Terminal/PowerShell

เปิด PowerShell หรือ Command Prompt แล้วไปที่โฟลเดอร์โปรเจกต์:

```bash
cd C:\Users\Acer\Desktop\Phulaport
```

### ขั้นตอนที่ 2: ตรวจสอบว่าเป็น Git Repository หรือยัง

```bash
git status
```

**ถ้าเห็น error:** ยังไม่ใช่ git repository → ต้อง init ก่อน

**ถ้าเห็น "On branch ...":** มี git repository แล้ว → ข้ามไปขั้นตอนที่ 4

### ขั้นตอนที่ 3: Initialize Git Repository (ถ้ายังไม่มี)

```bash
git init
```

### ขั้นตอนที่ 4: เพิ่มไฟล์ทั้งหมด

```bash
git add .
```

### ขั้นตอนที่ 5: Commit (บันทึกการเปลี่ยนแปลง)

```bash
git commit -m "Initial commit"
```

**หมายเหตุ:** ถ้าเป็นครั้งแรก อาจจะต้องตั้งค่า email/name ก่อน (ดูขั้นตอนที่ 2)

### ขั้นตอนที่ 6: เชื่อมต่อกับ GitHub Repository

**คัดลอก URL จาก GitHub** (หน้า repository ที่สร้างไว้)

URL จะเป็นแบบนี้: `https://github.com/username/Phulaport.git`

**ตัวอย่าง:**
```bash
git remote add origin https://github.com/YOUR_USERNAME/Phulaport.git
```

**แทนที่ `YOUR_USERNAME` ด้วย username ของคุณ**

### ขั้นตอนที่ 7: Push Code ไป GitHub

```bash
git branch -M main
git push -u origin main
```

**ถ้าเป็นครั้งแรก:** GitHub จะถาม username และ password
- **Username:** username ของคุณ
- **Password:** ใช้ **Personal Access Token** (ไม่ใช่ password จริง)

---

## 🔑 สร้าง Personal Access Token (สำหรับ Password)

### ถ้า Git ต้องการ Password:

1. ไปที่ GitHub → **Settings** (คลิกรูปโปรไฟล์)
2. ไปที่ **Developer settings** (ด้านล่างซ้าย)
3. คลิก **Personal access tokens** → **Tokens (classic)**
4. คลิก **"Generate new token"** → **"Generate new token (classic)"**
5. **ตั้งค่า:**
   - **Note:** "Vercel Deploy" (ชื่ออะไรก็ได้)
   - **Expiration:** เลือกระยะเวลา (90 days หรือ No expiration)
   - **Select scopes:** ✅ **repo** (เลือกทั้งหมดใน repo)
6. คลิก **"Generate token"**
7. **คัดลอก Token** (แสดงแค่ครั้งเดียว! เก็บไว้ให้ดี)
8. **ใช้ Token แทน Password** เมื่อ Git ถาม password

---

## 5. ตรวจสอบว่า Code อยู่บน GitHub แล้ว

1. ไปที่ repository บน GitHub: `https://github.com/YOUR_USERNAME/Phulaport`
2. คุณควรเห็นไฟล์ทั้งหมด:
   - `src/`
   - `package.json`
   - `vercel.json`
   - `index.html`
   - และไฟล์อื่นๆ

### ✅ เสร็จแล้ว! Code อยู่บน GitHub แล้ว

---

## 📝 สรุปคำสั่งทั้งหมด (Copy-Paste)

```bash
# 1. ไปที่โฟลเดอร์โปรเจกต์
cd C:\Users\Acer\Desktop\Phulaport

# 2. ตรวจสอบ git status
git status

# 3. ถ้ายังไม่มี git repository
git init

# 4. เพิ่มไฟล์ทั้งหมด
git add .

# 5. Commit
git commit -m "Initial commit"

# 6. เชื่อมต่อกับ GitHub (แทนที่ YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/Phulaport.git

# 7. Push ไป GitHub
git branch -M main
git push -u origin main
```

---

## 🔄 Push Code ใหม่ (หลังจากแก้ไขโค้ด)

เมื่อแก้ไขโค้ดแล้ว ต้องการ push ใหม่:

```bash
# 1. เพิ่มไฟล์ที่แก้ไข
git add .

# 2. Commit
git commit -m "อธิบายสิ่งที่แก้ไข"

# 3. Push
git push
```

---

## 🐛 แก้ไขปัญหา

### ปัญหา: "fatal: not a git repository"

**แก้ไข:**
```bash
git init
```

### ปัญหา: "Please tell me who you are"

**แก้ไข:**
```bash
git config --global user.name "ชื่อของคุณ"
git config --global user.email "your-email@example.com"
```

### ปัญหา: "remote origin already exists"

**แก้ไข:**
```bash
# ดู remote ที่มีอยู่
git remote -v

# ลบ remote เก่า
git remote remove origin

# เพิ่มใหม่
git remote add origin https://github.com/YOUR_USERNAME/Phulaport.git
```

### ปัญหา: Authentication failed

**แก้ไข:**
- ใช้ **Personal Access Token** แทน password
- หรือใช้ **GitHub Desktop** (GUI ที่ง่ายกว่า)

---

## 💡 Tips

- ✅ **Commit บ่อยๆ** เมื่อทำงานเสร็จแต่ละส่วน
- ✅ **เขียน commit message ที่ชัดเจน** เช่น "Add dark mode", "Fix navbar"
- ✅ **Push ไป GitHub ก่อน deploy** เพื่อให้ Vercel auto-deploy

---

## 🎯 ขั้นตอนต่อไป

หลังจาก push code ไป GitHub แล้ว:

1. ✅ ไปที่ [VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md) เพื่อ deploy บน Vercel
2. ✅ หรือดู [DEPLOY.md](./DEPLOY.md) สำหรับตัวเลือกอื่นๆ

---

**พร้อมแล้ว! ไป push code กันเลย! 🚀**

